# 专利质量分级项目总结

## 项目目标
使用RoBERTa+BiLSTM+Attention模型对专利进行质量分级（A/B/C三级）

## 已完成工作

### 1. 数据预处理 ✅
- **文件**: `data_preprocessing.py`
- **功能**: 
  - 读取原始专利数据（10,130条记录，85个属性）
  - 计算质量评分：专利被引次数/(min(专利到期日，2024年12月31日)-专利公开日期)
  - 根据评分分配质量标签：
    - A级（>1.85）：7,166条 (70.74%)
    - B级（0.85-1.85）：1,757条 (17.34%)
    - C级（<0.85）：1,204条 (11.89%)
  - 删除了专利被引次数等中间计算列
  - 生成了`patent_data_with_labels.xlsx`

### 2. 数据集划分 ✅
- **文件**: `split_dataset.py`
- **功能**:
  - 采用分层抽样，确保各类别比例一致
  - 90%训练集（9,114条）
  - 10%测试集（1,013条）
  - 生成了`train_data.xlsx`和`test_data.xlsx`

### 3. 模型架构实现 ✅
- **文件**: `roberta_bilstm_model.py`
- **架构设计**:
  ```
  输入文本 → RoBERTa编码器 → BiLSTM层 → Attention机制 → 分类器 → 质量标签(A/B/C)
  ```
- **关键组件**:
  - RoBERTa：使用中文预训练模型`hfl/chinese-roberta-wwm-ext`
  - BiLSTM：双向LSTM，隐藏层大小256，2层
  - Attention：自注意力机制，对BiLSTM输出进行加权
  - 分类器：全连接层，输出3个类别

### 4. 训练脚本 ✅
- **文件**: `train_model.py` (简化版测试)
- **超参数设置**:
  - Batch Size: 16
  - Learning Rate: 2e-5
  - Epochs: 10
  - Max Length: 256
  - Dropout: 0.3

## 生成的文件列表

```
autodl-tmp/
├── data_preprocessing.py      # 数据预处理脚本
├── split_dataset.py          # 数据集划分脚本
├── roberta_bilstm_model.py   # 完整模型实现
├── train_model.py            # 简化版训练脚本
├── patent_data_with_labels.xlsx  # 带标签的专利数据
├── train_data.xlsx           # 训练集
└── test_data.xlsx            # 测试集
```

## 下一步工作

### 方案1：在有网络的环境中运行
1. 确保能访问HuggingFace（huggingface.co）
2. 运行`python roberta_bilstm_model.py`进行完整训练
3. 模型将自动下载预训练权重并开始训练

### 方案2：离线模式
1. 先在有网络的环境下载模型：
   ```python
   from transformers import AutoTokenizer, AutoModel
   tokenizer = AutoTokenizer.from_pretrained('hfl/chinese-roberta-wwm-ext')
   model = AutoModel.from_pretrained('hfl/chinese-roberta-wwm-ext')
   tokenizer.save_pretrained('./roberta_local')
   model.save_pretrained('./roberta_local')
   ```
2. 修改代码中的模型路径为本地路径`./roberta_local`

### 方案3：使用镜像源
设置HuggingFace镜像：
```bash
export HF_ENDPOINT=https://hf-mirror.com
```

## 预期结果
- 模型训练完成后会生成：
