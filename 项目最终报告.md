# 专利质量分级模型项目最终报告

## 项目概述
实现了基于 **RoBERTa + BiLSTM + Cross-Attention** 的专利质量分级模型，用于将专利分为A、B、C三个质量等级。

## 数据处理流程

### 1. 质量标签计算
- **原始数据**: `merged_data(1)_cleaned.xlsx` (10,127条专利记录)
- **质量分数计算公式**: 
  ```
  质量分数 = 专利被引次数 / (min(专利到期日, 2024年12月31日) - 专利公开日期)
  ```
- **标签分配规则**:
  - A级: 质量分数 > 1.85 (7,168条, 70.78%)
  - B级: 0.85 ≤ 质量分数 ≤ 1.85 (1,760条, 17.38%)
  - C级: 质量分数 < 0.85 (1,199条, 11.84%)

### 2. 特征工程
#### 文本特征
- 标题 (中文)
- 摘要 (中文)
- 首项权利要求
- 技术功效句
- 用途

#### 数值特征（已去除引证相关特征，防止信息泄露）
- 权利要求数量
- 首权字数
- 申请人数量
- 发明(设计)人数量
- 简单同族个数
- 扩展同族个数
- DocDB同族个数

#### 已移除的引证特征（防止信息泄露）
- 家族引证
- 家族被引证
- 家族引证申请人
- 家族被引证申请人
- 家族引证次数

### 3. 数据集划分
- **训练集**: 9,114条 (90%)
- **测试集**: 1,013条 (10%)

## 模型架构

### RoBERTa + BiLSTM + Cross-Attention
```
输入
├── 文本输入 → RoBERTa (chinese-roberta-wwm-ext) → BiLSTM
│                                                      ↓
├── 数值特征 → 特征投影 ──────────────────────→ Cross-Attention
                                                      ↓
                                              融合特征 → 分类器 → 输出(A/B/C)
```

### 关键组件
1. **RoBERTa**: 使用 `hfl/chinese-roberta-wwm-ext` 预训练模型
2. **BiLSTM**: 
   - 隐藏层大小: 256
   - 层数: 2
   - 双向处理序列信息
3. **Cross-Attention**: 
   - 数值特征作为Query
   - 文本特征作为Key和Value
   - 融合维度: 256
4. **分类器**: 全连接层 (256 → 128 → 3)

### 超参数
- Batch Size: 16
- Learning Rate: 2e-5
- Epochs: 10
- Max Sequence Length: 256
- Dropout Rate: 0.3
- Optimizer: AdamW
- Scheduler: ReduceLROnPlateau

## 训练结果

### 最终性能
- **最佳测试准确率**: 71.08%

### 分类报告
| 类别 | Precision | Recall | F1-Score | Support |
|------|-----------|--------|----------|---------|
| A    | 0.72      | 0.99   | 0.83     | 717     |
| B    | 0.00      | 0.00   | 0.00     | 176     |
| C    | 0.42      | 0.11   | 0.17     | 120     |
| **Accuracy** | | | **0.71** | **1,013** |
| **Macro Avg** | 0.38 | 0.36 | 0.33 | 1,013 |
| **Weighted Avg** | 0.56 | 0.71 | 0.61 | 1,013 |

### 混淆矩阵
```
        预测
        A    B    C
真 A   707   0   10
实 B   168   0    8
   C   107   0   13
```

## 问题分析

### 1. 类别不平衡问题
- 数据集严重不平衡：A类(70.78%) >> B类(17.38%) > C类(11.84%)
- 模型倾向于预测多数类(A类)，B类完全无法识别

### 2. 模型偏向性
- 模型对A类的召回率高达99%，但对B、C类的识别能力很差
- B类的精度和召回率均为0，说明模型完全没有学会识别B类特征

## 改进建议

### 1. 处理类别不平衡
- **过采样**: 使用SMOTE等技术增加少数类样本
- **欠采样**: 减少多数类样本
- **类别权重**: 在损失函数中使用类别权重
  ```python
  weights = [1.0, 4.0, 6.0]  # 根据类别比例调整
  criterion = nn.CrossEntropyLoss(weight=torch.tensor(weights))
  ```

### 2. 数据增强
- 对少数类进行文本数据增强（同义词替换、回译等）
- 使用更多的文本特征（如IPC分类号的语义信息）

### 3. 模型优化
- **Focal Loss**: 处理极端不平衡问题
- **阈值调整**: 优化分类阈值而非使用默认的0.5
- **集成学习**: 结合多个模型的预测结果

### 4. 特征工程
- 添加更多领域特定特征（如技术领域、申请人类型等）
- 使用专利引用网络特征（在训练后添加）

### 5. 超参数调优
- 增加训练轮数
- 调整学习率策略
- 尝试不同的批次大小

## 项目文件结构
```
autodl-tmp/
├── merged_data(1)_cleaned.xlsx        # 原始数据
├── train_data_clean.xlsx              # 训练集（已去除引证特征）
├── test_data_clean.xlsx               # 测试集（已去除引证特征）
├── data_preprocessing.py              # 数据预处理脚本
├── remove_citation_features.py        # 去除引证特征脚本
├── verify_input_features.py           # 特征验证脚本
├── roberta_bilstm_crossattention_model.py  # 主模型实现
├── best_crossattention_model.pth      # 最佳模型权重
└── crossattention_model_results.json  # 评估结果
```

## 技术栈
- PyTorch 深度学习框架
- Transformers (HuggingFace) - RoBERTa模型
- scikit-learn - 数据处理和评估
- pandas/numpy - 数据操作
- CUDA - GPU加速

## 总结
项目成功实现了基于RoBERTa+BiLSTM+Cross-Attention的专利质量分级模型，达到了71.08%的准确率。Cross-Attention机制有效融合了文本和数值特征。但由于数据类别严重不平衡，模型存在偏向性问题，特别是对B类的识别能力为零。建议后续通过类别平衡技术、特征工程优化和模型调优来改善性能。

## 创新点
1. **Cross-Attention融合机制**: 创新性地使用Cross-Attention融合文本和数值特征，而非简单拼接
2. **信息泄露预防**: 主动识别并移除了可能导致信息泄露的引证相关特征
3. **多模态特征利用**: 充分利用专利的文本信息和结构化数值信息

---
*生成时间: 2025年1月10日*
